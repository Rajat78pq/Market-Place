export interface IProduct {
  productName: string;
  description: string;
  category: string;
  brand: string;
  price: number;
  discountPrice: number;
  stock: number;
  color: string;
  size: string;
  slug: string;
}
